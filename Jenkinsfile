pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "This is Jenkins integrated with Github."
                echo "Hello World!"
                echo "The World says hello back!"
                echo "Shame the configuration doesn't seem to work as expected, ONLY when scanning the triggers periodically..."
            }
        }
    }
}
